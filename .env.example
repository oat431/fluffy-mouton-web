# Local dev only — direct API access (bypasses gateway).
# In production, the app calls https://gateway.panomete.com/api/v1 automatically.
# Set this to your local API URL for development:
#   FLUMOU_API_URL=http://localhost:8004/api/v1
# Or to your local gateway:
#   FLUMOU_API_URL=http://localhost:8000/api/v1
FLUMOU_API_URL=
